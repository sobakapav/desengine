# dispatcher-install

Диспетчер установки, локального setup и системных preflight-проверок под `focus-tech`.

Этот change удерживает install/tooling-контур как отдельную техническую линию: локальная конфигурация, smoke-flow, onboarding sync и диагностические шаги перед запуском продукта.
