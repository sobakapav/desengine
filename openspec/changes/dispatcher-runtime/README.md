# dispatcher-runtime

Диспетчер укрепления runtime-границ лаборатории под `focus-tech`.

Этот change удерживает отдельную тактическую линию для lab runtime foundation: service boundary, mutation boundary, canonical route/runtime contracts и downstream cleanup/fix changes, усиливающих эти границы.
