# producer-project-mvp

Producer для первой delivery-волны `Project`: ввести сущность проекта как новый верхний контекст работы в desengine, сначала без roadmap-слоя, но с project-level `UI kit` как обязательным контрактом.
