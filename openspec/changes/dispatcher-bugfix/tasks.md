## Tasks

- [ ] 1. Зафиксировать границы bug-intake контура и область ответственности `dispatcher-bugfix`.
- [ ] 2. Описать допустимые входы для диспетчера:
  - [ ] 2.1 прямые жалобы и симптомы;
  - [ ] 2.2 ссылки на внешние страницы и тексты с жалобами;
  - [ ] 2.3 наблюдения о регрессиях и нарушении контракта.
- [ ] 3. Описать правила нормализации и маршрутизации багов:
  - [ ] 3.1 локальный баг уходит в дочерний `fix` под `dispatcher-bugfix`;
  - [ ] 3.2 доменный баг может быть оформлен как `fix` под другим профильным dispatcher'ом;
  - [ ] 3.3 неочевидный или массовый сигнал может сначала уйти в `producer`.
- [ ] 4. Зафиксировать требование к downstream bug changes:
  - [ ] 4.1 человеко-понятная формулировка наблюдаемой проблемы;
  - [ ] 4.2 ссылка на capability/scenario или явная фиксация contract gap;
  - [ ] 4.3 явная тестовая часть с уровнем проверки, командой запуска и данными.
- [ ] 5. Поддерживать handoff в состоянии, пригодном для передачи исполнения в downstream `fix` changes.

## Тестовая часть change

Затронутые OpenSpec capability/scenarios:
- `testing-layer`: downstream bugfix behavior-change обязан иметь явную тестовую часть и traceability-след.

Уровни проверки:
- static/contract: обязательный.
- unit: не требуется, dispatcher не меняет runtime.
- component/browser: не требуется.
- integration: не требуется.
- e2e smoke: не требуется.
- live/provider: не требуется.

Команды запуска:
- `npm run test:traceability`

Mock/fixture-данные и credentials:
- Не требуются: change документирует governance-слой intake и маршрутизации багов без runtime-изменений.

Если покрытие откладывается:
- Не требуется: dispatcher не вводит runtime behavior.
