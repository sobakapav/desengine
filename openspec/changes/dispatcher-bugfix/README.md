# dispatcher-bugfix

Диспетчер входящего потока багов внутри `focus-quality`.

Этот change принимает жалобы, симптомы и внешние ссылки, нормализует их в баговые формулировки, связывает с затронутыми capability/scenario и направляет в downstream `fix` changes: либо под собственным управлением, либо в ведение профильных dispatcher'ов.
