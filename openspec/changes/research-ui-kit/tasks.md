# Tasks: research-ui-kit

- [x] Привязать `research-ui-kit` к `focus-features` как стратегическому родителю.
- [x] Зафиксировать, что `dispatcher-ui-kit` и дочерние implement/fix changes ведутся в `focus-features`.
