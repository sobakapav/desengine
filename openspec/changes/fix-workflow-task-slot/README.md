# fix-workflow-task-slot

Убрать конечный каталог свободных задач для компонентов проекта. Компонент должен открывать типовой workflow как тип задачи, а не искать уникальный свободный task slot. Ситуация 'нет доступных задач' должна исчезнуть.
