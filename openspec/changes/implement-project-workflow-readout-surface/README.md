# implement-project-workflow-readout-surface

Вывести в пользовательский мир read-only workflow/artifact surface проекта: current workflow step, project-aware artifacts и runtime bindings между project, component workstream, workflow и locked workbench shell без изменения underlying orchestration.
