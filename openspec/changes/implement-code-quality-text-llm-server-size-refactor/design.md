## Решение

Декомпозиция LLM server идёт через внутренние modules, но публичный API остаётся совместимым. Live/provider credentials не используются: проверки должны опираться на существующие unit/mock сценарии.
