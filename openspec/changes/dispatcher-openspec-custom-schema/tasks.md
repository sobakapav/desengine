## Tasks

Технический backlog реализации ведётся в issue:
- https://github.com/sobakapav/desengine/issues/7

Ниже остаются продуктовые и traceability-задачи change.

- [ ] 1. Инвентаризировать текущие кастомные поля в changes (фактическое использование).
- [ ] 2. Зафиксировать MVP-расширение схемы (поля, типы, правила).
  - [ ] 2.1 Зафиксировать контракт `short` (маленькая буква в начале, максимум 75 символов, без пунктуации в конце)
  - [ ] 2.2 Зафиксировать поля `issue` и `review_sync_state` как часть продуктовой схемы
  - [ ] 2.3 Зафиксировать классификацию `focus/release/idea/research/dispatcher/implement/fix` и иерархические поля
- [ ] 3. Зафиксировать правила синхронизации OpenSpec ↔ GitHub Issues (MVP, без обязательной полной автоматики).
- [ ] 4. Определить источник truth схемы (где и как она хранится/валидируется).
- [ ] 5. Обновить внутренние инструменты под расширяемую схему:
  - [ ] 5.1 `openspec:new` шаблоны и базовая валидация
  - [ ] 5.2 `npm run openspec` (листинг) — вывод product-ориентированных полей
  - [ ] 5.3 статическая проверка схемы (`change_kind`, `execution_mode`, `parent_change`, `roadmap_ref`, `release_ref`)
- [ ] 6. Описать MVP-каскад закрытия change/task (issues, тесты, документация, кодовые ссылки).
- [ ] 7. План миграции существующих changes (поэтапно) + правила совместимости.
- [ ] 8. Тестовый план:
  - [ ] 8.1 unit проверки инструментов
  - [ ] 8.2 `npm run test:traceability` не деградирует

## Тестовая часть change

- [ ] Указать затронутые OpenSpec capability/scenarios (инструментальные): `admin-tools` / `Поле short в metadata change валидируется по кастомной схеме в режиме strict-v1` / `change связан с главным техническим issue`
- [ ] Выбрать уровень проверки: static/contract + unit
- [ ] Добавить или обновить тесты в общем слое тестирования
- [ ] Зафиксировать команду проверки: `npm run test:traceability` и `npm run test:unit -- change-testing-guidance` (дальше добавить проверку linkage с issue в отдельной задаче)
- [ ] Описать mock/fixture-данные и live credentials, если они нужны (для этого change не нужны)
- [ ] Если покрытие откладывается, добавить запись в `test/traceability/coverage-plan.json` с причиной и этапом закрытия
