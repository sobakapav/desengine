# dispatcher-openspec

OpenSpec-слой проекта как отдельный продуктовый и инструментальный контур; кастомная схема metadata — одно из его направлений.

Дополнительный инвариант этого change: roadmap принадлежат стратегическим changes и наследуются downstream dispatcher-ами, а не живут как локальные тактические файлы.
