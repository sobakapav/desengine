## Why

Нужен исполнительский change для реализации задачи parent owner.

## What Changes

- Реализовать: Сделать workflow-пункт реальным механизмом управления генерацией артефактов: selected point должен ограничивать primary file set и менять start/iterate поведение так, чтобы пользователь мог целенаправленно догенерировать component, styles, mock, props или storybook.

## Impact

- Изменение закрывает конкретный исполнительский срез в рамках текущего parent owner.
