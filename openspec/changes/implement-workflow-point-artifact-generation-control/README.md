# implement-workflow-point-artifact-generation-control

Сделать workflow-пункт реальным механизмом управления генерацией артефактов: selected point должен ограничивать primary file set и менять start/iterate поведение так, чтобы пользователь мог целенаправленно догенерировать component, styles, mock, props или storybook.
