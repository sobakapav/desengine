## Tasks

- [ ] 1. Зафиксировать UX-принцип: пользовательский путь к завершению уровня описывается через единое состояние `Проверка пройдена`, а не через две отдельные психологические фазы.
- [ ] 2. Определить downstream capability/scenarios, которые нужно уточнить в behavior-change:
  - [ ] 2.1 `task-levels`
  - [ ] 2.2 `user-progress`
  - [ ] 2.3 `task`
- [ ] 3. Подготовить отдельный downstream change реализации под `dispatcher-ux`, который уточнит:
  - [ ] 3.1 новые тексты CTA, статусов и экранов;
  - [ ] 3.2 правила перехода от генерации к проверке;
  - [ ] 3.3 критерий, по которому пользователь видит, сколько осталось до `Проверка пройдена`;
  - [ ] 3.4 сценарий неуспешной проверки как продолжение единого цикла доработки.
- [ ] 4. Проверить, что downstream change не ослабляет текущий контракт отдельной успешной проверки уровня, а меняет только UX-модель восприятия и навигации.
- [ ] 5. Зафиксировать тестовую интеграцию будущего behavior-change в единый слой тестирования.

## Тестовая часть change

- [ ] Указать затронутые OpenSpec capability/scenarios: `task-levels`, `user-progress`, `task`.
- [ ] Выбрать уровень проверки для downstream behavior-change: static/contract + unit; при изменении экранов дополнить component/browser или e2e smoke.
- [ ] Добавить или обновить тесты в общем слое тестирования.
- [ ] Зафиксировать команду проверки: минимум `npm run test:traceability`, далее релевантные `npm run test:unit` и/или `npm run test:e2e`.
- [ ] Описать mock/fixture-данные и live credentials, если они нужны.
- [ ] Если покрытие откладывается, добавить запись в `test/traceability/coverage-plan.json` с причиной и этапом закрытия.
