## Context

Жалоба хорошо локализована и опирается на конкретную ошибку ОС: `EXDEV`. В текущем коде действительно используется `rename` поверх временного checkout.

## Goals

- Переживать cross-device move без падения smoke/repair flow.
- Не допускать ложной маркировки onboarding как синхронизированного.

## Non-goals

- Не переписывать весь onboarding pipeline.

## Decisions

1. Исправление должно покрыть и основной smoke-flow, и соседний repair-flow.
2. Успешная синхронизация подтверждается только после завершённого safe move.
