## Context

Это одна из самых прямых жалоб в документе: root cause уже локализован, а код в текущей ветке его подтверждает.

## Goals

- Починить setup-flow без обходных ручных действий.
- Свести к одному каноническому пути чтение локального конфига в tooling-слое.

## Non-goals

- Не переделывать весь local-config API.

## Decisions

1. Исправление нужно провести сразу во всех затронутых tool-entry points.
2. После фикса должна быть проверка на отсутствие legacy import path в критичных setup-tools.
