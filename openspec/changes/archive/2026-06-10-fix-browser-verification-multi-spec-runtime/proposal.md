## Why

Нужен исполнительский change для реализации задачи диспетчера.

## What Changes

- Реализовать: Починить browser verification runtime wrapper, чтобы команда проверяла все переданные spec-файлы, а не только первый

## Impact

- Изменение закрывает конкретный исполнительский срез в рамках текущего dispatcher.
