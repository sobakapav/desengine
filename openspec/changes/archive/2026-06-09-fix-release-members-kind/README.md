# fix-release-members-kind

Локальный fix-change под `dispatcher-openspec`: release может держать только исполнительский состав `implement`/`fix`, а попытки привязать к релизу `focus`/`idea`/`producer`/`dispatcher`/`release` должны считаться ошибкой OpenSpec metadata.
