# fix-monaco-cancellation-noise

Локальный bugfix-change под `dispatcher-bugfix`, который убирает ложный browser/dev overlay с `Canceled` от Monaco, не скрывая реальные runtime-ошибки редактора или лаборатории.
