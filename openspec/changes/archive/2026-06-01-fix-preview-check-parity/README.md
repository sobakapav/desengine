# fix-preview-check-parity

Исполнительский `fix`-change под `dispatcher-bugfix`.

Чинит пользовательски заметный drift: задача не должна одновременно показывать broken preview и проходить проверку как успешная без явного объяснения этого расхождения.
