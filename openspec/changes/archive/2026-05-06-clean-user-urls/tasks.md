## 1. URL-карта и маршрутные контракты

- [x] 1.1 Зафиксировать каноническую top-level URL-карту: `/`, `/tasks`, `/levels`, `/auth`, `/system`, `/help`
- [x] 1.2 Зафиксировать path-based схему для задачи: `/tasks/<taskId>` и `/tasks/<taskId>/<screen>`
- [x] 1.3 Зафиксировать отдельный маршрут `/tasks/<taskId>/next` для перехода между уровнями
- [x] 1.4 Зафиксировать path-based схему для уровней: `/levels` и `/levels/<levelId>`
- [x] 1.5 Зафиксировать, что пользовательские URL не используют query-параметры как официальный навигационный контракт

## 2. Auth-flow и восстановление контекста

- [x] 2.1 Описать redirect-поведение `/` на `/tasks`
- [x] 2.2 Описать правило, по которому пользователь без допуска попадает на `/auth`
- [x] 2.3 Зафиксировать механизм возврата после допуска на целевой path без `?next=...`
- [x] 2.4 Зафиксировать правило валидации допустимых return path для post-auth возврата

## 3. Legacy cleanup

- [x] 3.1 Вывести `/lab?screen=...` и другие query-driven пользовательские маршруты из официального контура
- [x] 3.2 Обновить связанные OpenSpec-спеки и документацию под новую публичную URL-схему
- [x] 3.3 Зафиксировать, что `screen` совпадает с кодом допустимого на этом уровне файла, а при отсутствии `screen` открывается `Component`
