# Задачи

## 1. Растянуть Sandpack на всю ширину

- [x] Добавить `width: 100%` для `SandpackPreview` и wrapper `w-full` в `components/desengine/lab/InOut/OutRender/OutRender.tsx`.
- [x] Добавить `width: 100%` в глобальные `.sp-*` классы в `app/globals.css`.

## 2. Зафиксировать контракт тестом

- [x] Обновить `test/unit/p1-source-contracts.test.ts`, чтобы проверять наличие `width: "100%"` в `OutRender`.
- [x] Обновить OpenSpec сценарий "Пользователь открывает рабочий экран на desktop" (добавить требование про ширину результата).

## 3. Прогон проверки

- [x] `npm run test:unit`.

