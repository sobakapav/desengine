## Подключение Chakra UI

Интеграция выполняется через единый слой Sandpack UI kit'ов:

- конфиг: `lib/lab/sandpack-ui-kits.config.ts`;
- зависимости: корневой `package.json`;
- дополнительные импорты (если нужны): через `indexTsxImports`.

