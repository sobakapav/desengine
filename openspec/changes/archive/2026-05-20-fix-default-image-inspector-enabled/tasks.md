# Tasks: fix-default-image-inspector-enabled

## 1. Контракт

- [x] 1.1 Обновить OpenSpec delta по `level-labs`/`task`: инспектор включён по умолчанию.
- [x] 1.2 Зафиксировать override-поведение через query-параметр (`imageInspector=0|off|false`).

## 2. Реализация

- [x] 2.1 Обновить клиентскую логику выбора режима рендера картинки в лаборатории.
- [x] 2.2 Сохранить обратную совместимость URL-параметра как явного override.
- [x] 2.3 Стабилизировать lifecycle инспектора, чтобы показ картинки не зависел от первого resize контейнера.
- [x] 2.4 Использовать размеры изображения из конфига задачи для стартового рендера инспектора.
- [x] 2.5 Оставить zoom-контур доступным, но убрать автоматический fit при первом показе картинки.

## 3. Проверки

- [x] 3.1 Добавить/обновить unit или source-contract тест на ветку «инспектор по умолчанию включён».
- [x] 3.2 Добавить тест на ветку «параметр отключает инспектор».
- [x] 3.3 Запустить `npm run test:unit`.
- [x] 3.4 Запустить `npm run test:traceability`.

## 4. Приёмка

- [x] 4.1 Без query-параметров лаборатория показывает инспектор изображения.
- [x] 4.2 При `imageInspector=0|off|false` лаборатория показывает обычный `<Image>` fallback.
- [x] 4.3 После первой загрузки картинка не мигает и не возвращается к бесконечной надписи «Загрузка изображения…».
- [x] 4.4 При первом показе инспектор открывает картинку в масштабе `1:1`, а не растягивает её автоматически.
