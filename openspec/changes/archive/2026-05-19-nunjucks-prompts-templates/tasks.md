## 1. Инвентаризация текущей системы промптов

- [x] 1.1 Зафиксировать, какие промпты читаются с диска и где склеиваются в коде (production/didactic + levels start/iterate/check).
- [ ] 1.2 Сформировать список повторяющихся блоков (общие правила, формат ответа, правила работы с файлами, правила дизайн-системы).

## 2. Введение Nunjucks-рендера (совместимость)

- [x] 2.1 Добавить Nunjucks в зависимости и создать модуль рендера промптов (loader + render).
- [x] 2.2 Перевести чтение промптов на `.njk` и обеспечить режим совместимости (контент без изменений → тот же output).
- [x] 2.3 Поддержать рендер для level-промптов (`levels/<id>/{start,iterate,check}.njk`) с тем же контрактом ошибок (start обязателен, iterate/check опциональны).
- [x] 2.4 Определить правила кэширования (prod) и поведения в dev (без кэша или сброс по запросу).

## 2.1 Наследование (обязательная поддержка)

- [x] 2.1.1 Зафиксировать базовый layout-шаблон (например, `base.njk`) и схему блоков.
- [x] 2.1.2 Включить `extends/blocks` в основные промпты (start/iterate/check) как целевой формат.

## 3. Контекст (переменные окружения) для промптов

- [ ] 3.1 Определить тип/схему `PromptRenderContext` (user/task/level).
- [ ] 3.2 Определить источник `user`-контекста (где хранится активная дизайн-система и др. настройки).
- [ ] 3.3 Протащить контекст до мест, где собирается instruction (start/iterate/check) без изменения смысла (на первом шаге — пустой/минимальный контекст).

## 4. Миграция сборки из кода в шаблоны

- [x] 4.1 Создать `partials/` и вынести туда общие блоки.
- [x] 4.2 Переписать верхнеуровневые промпты как Nunjucks-шаблоны, которые включают partials.
- [ ] 4.3 Удалить/упростить участки кода, отвечающие за склейку «секций», если они становятся чисто шаблонными.

## 5. Тестовая часть change (обязательная)

- [ ] Указать затронутые OpenSpec capability/scenarios
  - capability: `task`
    - scenario: "Старт уровня формирует instruction из промптов и контекста"
    - scenario: "Итерация уровня формирует instruction из промптов и контекста"
    - scenario: "Проверка уровня формирует instruction из промптов и контекста"
- [ ] Выбрать уровень проверки: unit + static/traceability
- [ ] Добавить/обновить тесты в общем слое тестирования
  - unit: тесты на `renderPrompt(...)` (совместимость: старые промпты == новые при пустом контексте)
  - unit: тесты на include/partials (один-два «сквозных» примера)
  - unit: тесты на `extends/blocks` (один «сквозной» пример наследования)
  - unit: тесты на обработку ошибок (отсутствие start для levelId → ошибка; iterate/check → пусто)
- [ ] Зафиксировать команду проверки: `npm run test:unit && npm run test:traceability`
- [ ] Описать mock/fixture-данные и live credentials, если они нужны
  - fixtures: промпты из репозитория; дополнительные фикстуры — только для unit (если потребуется временный каталог)
  - live credentials не нужны
- [ ] Если покрытие откладывается, добавить запись в `test/traceability/coverage-plan.json` с причиной и этапом закрытия

## 6. OpenSpec / документация

- [x] 6.1 Обновить `openspec/specs/**`, если меняется действующий контракт промптов (формат, переменные, структура путей).
- [x] 6.2 Добавить короткую документацию для авторов промптов: поддерживаемый синтаксис, доступные переменные, примеры include/partials.
