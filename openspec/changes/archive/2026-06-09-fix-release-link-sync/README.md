# fix-release-link-sync

Локальный fix-change под `dispatcher-openspec`: release-диспетчеризация должна привязывать change к релизу полностью, синхронно обновляя `.openspec.yaml` и `handoff.md`, а не оставлять полусостояние.
