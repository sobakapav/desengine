# Адаптер DeepSeek

## Purpose

Зафиксировать требования к отдельному адаптеру DeepSeek в общем LLM-слое продукта.

## Requirements

### Requirement: Система поддерживает адаптер DeepSeek

Система SHALL поддерживать отдельный адаптер для DeepSeek.

#### Scenario: Оператор выбирает DeepSeek
- **WHEN** конфигурация приложения переключена на DeepSeek
- **THEN** система использует адаптер DeepSeek для LLM-вызовов

### Requirement: Конфигурация DeepSeek задаётся явно

Система SHALL брать параметры подключения к DeepSeek из локальной конфигурации приложения.

#### Scenario: Оператор настраивает DeepSeek
- **WHEN** оператор задаёт обязательные параметры подключения к DeepSeek
- **THEN** система использует их без изменения кода

#### Scenario: DeepSeek включён как активный провайдер
- **WHEN** оператор задаёт `LLM_PROVIDER=deepseek`
- **AND** задаёт `DEEPSEEK_API_KEY`, `DEEPSEEK_MODEL` и при необходимости `DEEPSEEK_BASE_URL`
- **THEN** система использует именно эти параметры для LLM-вызовов

### Requirement: Ошибки DeepSeek различаются по типам

Система SHALL различать как минимум следующие классы ошибок для DeepSeek:
- неполная конфигурация;
- ошибка авторизации;
- недоступность endpoint;
- таймаут;
- невалидный ответ провайдера.

#### Scenario: Авторизация DeepSeek не прошла
- **WHEN** провайдер возвращает ошибку авторизации
- **THEN** пользователь видит понятное сообщение об ошибке доступа к DeepSeek
