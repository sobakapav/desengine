# Лаборатории уровней (Level Labs)

Лаборатория уровня определяет, как пользователь работает с задачей на конкретном уровне: какие вводные показываются, какие файлы можно редактировать и какой редактор используется для этих файлов.

## Purpose

Зафиксировать контракт лаборатории уровня как отдельной capability: набор картинок, набор разрешённых файлов, UI-форму лаборатории и поведение редактора.
## Requirements
### Requirement: Лаборатория имеет стабильный runtime-контракт маршрутов

Система SHALL использовать единый canonical route map для рабочих маршрутов лаборатории и связанных task transition экранов.

#### Scenario: Пользователь открывает рабочую задачу лаборатории
- **WHEN** пользователь открывает рабочий маршрут задачи
- **THEN** canonical рабочим URL считается `/lab/<taskId>`
- **AND** route/page слой строит этот URL через общий runtime helper
- **AND** результат проверки из этого маршрута открывается внутри текущего lab-flow без обязательного промежуточного route-hop на отдельную страницу

#### Scenario: Пользователь открывает рабочий файл задачи
- **WHEN** пользователь открывает разрешённый рабочий экран задачи
- **THEN** canonical URL строится как `/lab/<taskId>/<screen>`
- **AND** экран проверяется по allowlist текущего уровня

#### Scenario: Legacy route ведёт к transition экрану
- **WHEN** пользователь открывает legacy lab route для check/done/next
- **THEN** система использует явный compatibility redirect к canonical task transition route
- **AND** redirect покрыт тестом маршрутизации или source-contract проверкой

### Requirement: Лаборатория строит пустое состояние задачи единым способом

Система SHALL использовать reusable factory для пустого `TaskData`, чтобы route/page/API слои не копировали shape вручную.

#### Scenario: Задача ещё не стартовала
- **WHEN** runtime должен вернуть данные ещё не начатой задачи
- **THEN** он строит `TaskData` через общий helper
- **AND** `contentByFileId`, `promptHistory`, `llmUsageSummary` и `labContext` имеют стабильный shape

#### Scenario: Лаборатория получает retriable overload-отказ task action runtime
- **WHEN** Workbench вызывает `save files`, `reset task` или `reset current level`
- **AND** task action runtime уже достиг bounded overload budget
- **THEN** route boundary возвращает retriable overload-ошибку
- **AND** текущий Workbench state не теряет уже применённые данные
- **AND** действие не оставляет частично поставленную в очередь мутацию

### Requirement: Task screen распространяет один screen event contract

Система SHALL собирать для MVP task/workbench flow один screen-level event contract и передавать его по цепочке `page -> LabScreen -> TaskScreenSection -> Workbench -> child consumer` без второго ad-hoc shape.

#### Scenario: Пользователь открывает рабочий файл задачи
- **WHEN** `app/lab/<taskId>/<screen>` открывает task screen
- **THEN** route/page слой собирает один screen event input для текущего `taskId` и `activeScreen`
- **AND** `LabScreen` строит из него единый screen event contract
- **AND** `TaskScreenSection` и `Workbench` передают этот contract дальше без локального дублирования shape

#### Scenario: Пользователь редактирует один файл и переключается на другой
- **WHEN** child consumer внутри `Workbench` меняет `activeScreen`
- **THEN** обновление проходит через тот же screen event contract
- **AND** следующий active screen наблюдается потомком без второго параллельного пути состояния
- **AND** MVP-реализация не расширяется на `check`, `done` и `transition`

### Requirement: Уровень 1 имеет собственную лабораторию

Система SHALL предоставлять отдельную лабораторию для уровня 1.

#### Scenario: Пользователь открывает задачу уровня 1
- **WHEN** пользователь открывает задачу на уровне 1
- **THEN** система открывает лабораторию уровня 1

### Requirement: Лаборатория уровня 1 показывает только base-картинку

Система SHALL показывать в лаборатории уровня 1 только `base.png`, заданную глобальным контрактом уровня 1.

#### Scenario: Открыта задача уровня 1
- **WHEN** лаборатория уровня 1 показывает визуальные вводные
- **THEN** система показывает `base.png`
- **AND** не показывает `variants.png`
- **AND** не показывает техническое имя файла рядом с preview

### Requirement: Лаборатория уровня 2 имеет собственную лабораторию

Система SHALL предоставлять отдельную лабораторию для уровня 2.

#### Scenario: Пользователь открывает задачу уровня 2
- **WHEN** пользователь открывает задачу на уровне 2
- **THEN** система открывает лабораторию уровня 2

### Requirement: Лаборатория уровня 2 показывает base и variants

Система SHALL показывать в лаборатории уровня 2 обе картинки, заданные глобальным контрактом уровня 2: `base.png` и `variants.png`.

#### Scenario: Открыта задача уровня 2
- **WHEN** лаборатория уровня 2 показывает визуальные вводные
- **THEN** система показывает `base.png`
- **AND** показывает `variants.png`
- **AND** не показывает технические имена файлов рядом с preview

### Requirement: Формат лаборатории уровня задаётся глобальным level-config

Система SHALL задавать для каждого уровня глобальный level-config, который определяет:
- список показываемых и отправляемых в инициирующий промпт картинок;
- список редактируемых файлов;
- идентификатор лаборатории уровня.

Линейные размеры картинок SHALL браться не из глобального level-config, а из task-specific конфигурации задачи по именованным типам картинок (`base`, `variants` и т.д.).

#### Scenario: Система загружает контракт лаборатории уровня
- **WHEN** система загружает данные задачи для конкретного уровня
- **THEN** она берёт общий формат лаборатории из глобального level-config этого уровня

### Requirement: Level-config имеет фиксированный минимальный shape

Система SHALL использовать для global level-config как минимум следующие поля:
- `levelId`;
- `labId`;
- `images[]` с элементами `{ id, show, sendToInitPrompt }`;
- `editableFileIds[]`.
- необязательный `url` для открытой ссылки уровня.

#### Scenario: Система читает level-config уровня
- **WHEN** система загружает global level-config
- **THEN** она может определить по нему, какую лабораторию открыть, какие картинки показать, какие картинки отправить в инициирующий промпт и какие файлы разрешено редактировать
- **AND** при наличии получает открытый `url` уровня

### Requirement: Открытый config уровня поддерживает URL

Система SHALL позволять уровню хранить дополнительный открытый параметр `url` в `onboarding/levels/<levelId>/config.json`.

#### Scenario: Система читает config уровня с URL
- **WHEN** в `config.json` уровня задано поле `url`
- **THEN** система принимает его как часть открытых метаданных уровня

### Requirement: URL уровня может отсутствовать

Система SHALL считать URL уровня необязательным параметром открытого level-config.

#### Scenario: Уровень не имеет заданного URL
- **WHEN** в `config.json` уровня нет поля `url` или оно пустое
- **THEN** уровень всё равно считается валидным
- **AND** система не падает при чтении level-config

### Requirement: Каждый уровень хранится в собственной onboarding-папке

Система SHALL хранить данные каждого уровня в отдельной папке `onboarding/levels/<levelId>/`.

Внутри папки уровня SHALL существовать как минимум:
- `config.json` с компактным контрактом уровня;
- `overview.md` с общим пояснением уровня в Markdown-формате.

#### Scenario: Система загружает данные уровня
- **WHEN** система читает данные уровня `L`
- **THEN** она читает `config.json` из папки этого уровня
- **AND** читает общее пояснение из `overview.md`
- **AND** не читает hidden prompt-материалы из этой папки

#### Scenario: Система показывает общее пояснение уровня пользователю
- **WHEN** система выводит `overview.md` на странице уровня, в общем списке уровней или на стартовом экране уровня задачи
- **THEN** она рендерит содержимое файла как Markdown

### Requirement: Markdown уровня поддерживает ссылки и картинки

Система SHALL поддерживать в `overview.md` уровня как минимум ссылки и изображения.

#### Scenario: В overview уровня есть ссылка
- **WHEN** в markdown-описании уровня присутствует ссылка
- **THEN** пользователь видит кликабельную ссылку

#### Scenario: В overview уровня есть картинка
- **WHEN** в markdown-описании уровня присутствует изображение
- **THEN** пользователь видит встроенную картинку в HTML-рендере

### Requirement: Страница уровня показывает ссылку на дополнительные материалы

Система SHALL показывать на странице уровня ссылку на дополнительные материалы, если она задана в конфиге уровня.

#### Scenario: У уровня задан URL дополнительных материалов
- **WHEN** пользователь открывает страницу уровня
- **THEN** система показывает ссылку на дополнительные материалы этого уровня

### Requirement: JSON уровня не дублирует текстовые материалы

Система SHALL не хранить в `config.json` уровня большое общее описание уровня и hidden level prompts.

#### Scenario: Разработчик открывает config уровня
- **WHEN** разработчик читает `onboarding/levels/<levelId>/config.json`
- **THEN** он видит только компактный контракт уровня
- **AND** не видит дублирования содержимого `overview.md` и hidden prompt-файлов вроде `start.md` и `iterate.md`

### Requirement: Набор редактируемых файлов зависит от уровня

Система SHALL задавать список редактируемых файлов через global level-config уровня.

Для уровня 1 разрешены только:
- `Component.tsx`;
- `Component.stories.tsx`.

Для уровня 2 разрешены:
- `Component.tsx`;
- `Component.stories.tsx`;
- `styles.ts`.

#### Scenario: Пользователь открывает задачу уровня 1
- **WHEN** система показывает доступные для редактирования файлы уровня 1
- **THEN** пользователь может редактировать только `Component.tsx` и `Component.stories.tsx`

#### Scenario: Пользователь открывает задачу уровня 2
- **WHEN** система показывает доступные для редактирования файлы уровня 2
- **THEN** пользователь может редактировать `Component.tsx`, `Component.stories.tsx` и `styles.ts`

### Requirement: У задачи остаётся task-specific пояснение уровня

Система SHALL хранить отдельно от глобального level-config уникальное для задачи пояснение текущего уровня.
Если task-specific пояснение доступно, рабочий экран SHALL показывать именно его и не дублировать рядом общее описание уровня.

#### Scenario: Пользователь открывает конкретную задачу на уровне
- **WHEN** система показывает лабораторию уровня
- **THEN** она показывает общее пояснение уровня
- **AND** task-specific пояснение этой задачи

#### Scenario: Система показывает task-specific пояснение уровня пользователю
- **WHEN** система выводит `onboarding/tasks/<taskId>/levels/<levelId>/tip.md`
- **THEN** она рендерит содержимое файла как Markdown

#### Scenario: Пользователь открыл задачу внутри уровня
- **WHEN** задача имеет собственные пользовательские детали текущего уровня
- **THEN** рабочий экран задачи показывает именно их
- **AND** не дублирует рядом общее описание уровня

### Requirement: Task config хранит размеры картинок

Система SHALL хранить в task-specific конфигурации задачи:
- размеры картинок по именованным типам (`base`, `variants` и т.д.).

#### Scenario: Система загружает task-specific данные задачи
- **WHEN** система открывает задачу на конкретном уровне
- **THEN** она берёт размеры нужных картинок из task config

#### Scenario: Пользователь видит референс и результат
- **WHEN** рабочий экран задачи показывает preview-блок
- **THEN** система не показывает размеры картинок как отдельную пользовательскую подпись
- **AND** не оборачивает область результата лишней рамкой, если она не несёт отдельного смысла

#### Scenario: Пользователь открывает рабочий экран на desktop
- **WHEN** ширина экрана соответствует desktop-режиму
- **THEN** preview и результат выравниваются горизонтально
- **AND** воспринимаются как часть единого рабочего полотна, а не как две вложенные тяжёлые карточки
- **AND** Sandpack preview в блоке результата растягивается на всю доступную ширину своей колонки

### Requirement: Лаборатория может включать инспектор изображений

Система SHOULD поддерживать интерактивный просмотр вводных картинок в лаборатории через `image-inspector`.

На этапе поэтапного включения лаборатория MAY включать инспектор через query-параметр `imageInspector=1`.

#### Scenario: Пользователь включает инспектор изображения в лаборатории
- **WHEN** пользователь открывает лабораторию с `imageInspector=1`
- **THEN** система показывает интерактивный инспектор для вводной картинки

### Requirement: Sandpack preview использует App template уровня

Система SHALL собирать Sandpack preview так, чтобы оболочка рендера (`App.tsx`) была определена уровнем, а не общим runtime-кодом.

Каноническое место шаблона уровня:
- `onboarding/levels/<levelId>/sandpack/App.tsx`.

Опционально уровень MAY добавлять CSS для preview:
- `onboarding/levels/<levelId>/sandpack/preview.css`.

`preview.css` уровня SHALL добавляться к базовому runtime CSS предпросмотра (не заменяя его полностью), чтобы сохранить общий набор переменных и reset-правил.

#### Scenario: Система выбирает Sandpack App template по уровню задачи
- **WHEN** система готовит payload для Sandpack preview задачи
- **THEN** она определяет уровень, которому принадлежит preview:
  - текущий уровень задачи, если он уже начат;
  - предыдущий уровень задачи, если текущий уровень ещё не начат (и это не уровень 1).
- **AND** система пытается прочитать `onboarding/levels/<levelId>/sandpack/App.tsx`
- **AND** если файл существует, она использует его как `App.tsx` в виртуальном проекте
- **AND** если файла нет, она использует совместимый shared fallback-template

#### Scenario: Preview собирает mock-данные из `mock.ts`
- **WHEN** Sandpack preview рендерит компонент по `mock.ts`
- **THEN** он сначала собирает все object-like named exports из `mock.ts`, кроме `mock`, и рендерит их по очереди
- **AND** если таких exports нет, использует `mock`

### Requirement: Лаборатория использует Monaco для редактируемых файлов

Система SHALL использовать Monaco Editor как основной редактор для файлов, разрешённых к редактированию в лаборатории уровня.

#### Scenario: Пользователь открывает лабораторию уровня
- **WHEN** у уровня есть редактируемые файлы
- **THEN** система показывает эти файлы через Monaco Editor

#### Scenario: Monaco штатно отменяет внутреннюю async-операцию
- **WHEN** Monaco отменяет собственную внутреннюю async-операцию во время dispose, remount или смены модели
- **AND** редактор при этом остаётся рабочим
- **THEN** система не показывает пользователю ложный overlay как признак падения лаборатории
- **AND** suppression не распространяется на посторонние runtime-ошибки вне Monaco cancellation

### Requirement: Переключение файлов сохраняет текущее состояние редактирования

Система SHALL корректно сохранять текущее содержимое активного файла в состоянии страницы при переключении между файлами.

#### Scenario: Пользователь редактирует один файл и переключается на другой
- **WHEN** пользователь изменил содержимое активного файла и открыл другую вкладку файла
- **THEN** система показывает новый файл
- **AND** не теряет текущий текст первого файла в состоянии задачи

### Requirement: Monaco имеет fallback

Система SHALL сохранять возможность редактирования даже если Monaco не загрузился или не инициализировался.

#### Scenario: Monaco не загрузился
- **WHEN** основной редактор Monaco недоступен
- **THEN** система переключается на fallback-режим редактирования
- **AND** пользователь всё ещё может править файл и сохранить изменения

### Requirement: Первый ввод в редакторе не теряется при переходе файла в dirty-state

Система SHALL сохранять первое пользовательское изменение редактируемого файла полностью, даже если именно это изменение впервые переводит файл в состояние с несохранёнными правками и показывает действие `Сохранить`.

#### Scenario: Пользователь вводит первый символ в чистом файле
- **WHEN** активный редактируемый файл ещё не имеет несохранённых изменений
- **AND** пользователь вводит первый символ в редакторе
- **THEN** система сохраняет этот символ в текущем содержимом файла
- **AND** помечает файл как несохранённый
- **AND** показывает действие `Сохранить` без потери введённого символа

#### Scenario: Пользователь впервые вставляет текст в чистый файл
- **WHEN** активный редактируемый файл ещё не имеет несохранённых изменений
- **AND** пользователь вставляет в редактор текстовый фрагмент
- **THEN** система сохраняет весь вставленный фрагмент в текущем содержимом файла
- **AND** помечает файл как несохранённый
- **AND** показывает действие `Сохранить` без обрезания вставленного текста

#### Scenario: Основной редактор недоступен
- **WHEN** основной редактор Monaco недоступен
- **AND** пользователь начинает редактировать файл через fallback-режим
- **THEN** первый пользовательский ввод также не теряется

### Requirement: Стартовый экран уровня 1 показывает допустимые для уровня картинки

Система SHALL на стартовом экране уровня 1 конкретной задачи показывать все картинки уровня 1, которые помечены как показываемые в глобальном level-config этого уровня.

#### Scenario: Пользователь открывает стартовый экран уровня 1 задачи
- **WHEN** пользователь открывает стартовый экран уровня 1 конкретной задачи
- **THEN** система показывает все картинки из `images[]` глобального level-config уровня 1, у которых `show=true`
- **AND** не показывает картинки, у которых `show=false`
- **AND** не показывает технические имена файлов рядом с preview

### Requirement: Стартовый экран уровня L>1 показывает допустимые картинки текущего уровня и рендер предыдущего уровня

Система SHALL на стартовом экране уровня `L>1` конкретной задачи показывать:

- картинки текущего уровня `L`, допустимые к показу по глобальному level-config;
- рендер результата предыдущего уровня `L-1` как визуальный контекст.

#### Scenario: Пользователь открывает стартовый экран уровня 2+ задачи
- **WHEN** пользователь открывает стартовый экран уровня `L>1` конкретной задачи
- **THEN** система показывает все картинки из `images[]` глобального level-config уровня `L`, у которых `show=true`
- **AND** показывает блок рендера результата уровня `L-1`

#### Scenario: Результат предыдущего уровня недоступен
- **WHEN** пользователь открывает стартовый экран уровня `L>1` конкретной задачи
- **AND** результат уровня `L-1` недоступен
- **THEN** система показывает понятный fallback для блока предыдущего уровня
- **AND** стартовый экран остаётся рабочим (не падает и не блокирует пользователя)

### Requirement: Лаборатория использует Project Workspace для Sandpack preview

Система SHALL использовать `ProjectWorkspace` для лаборатории, чтобы настройки Sandpack preview были привязаны к project boundary, а не только к глобальному env/config.

#### Scenario: Лаборатория создаёт локальный проект для preview
- **WHEN** пользователь открывает рабочую лабораторию задачи
- **THEN** система создаёт локальный `ProjectWorkspace` с `id`, `title`, `createdAt`, `updatedAt` и `settings`
- **AND** `uiKitId` и `uiMode` сохраняются внутри `ProjectWorkspace.settings`

#### Scenario: Лаборатория сохраняет локальные project settings при rehydration
- **WHEN** лаборатория читает локальный `ProjectWorkspace` из browser storage
- **THEN** система сохраняет прочитанные `project.settings.uiKitId` и `project.settings.uiMode`, если они валидны
- **AND** preview наследует текущие project settings без принудительного перевода в `shadcn/ui-kit`
- **AND** не создаёт новый несовместимый project shape поверх сохранённого `ProjectWorkspace`

#### Scenario: Пользователь переключает UI kit проекта без перезагрузки страницы
- **WHEN** пользователь меняет `project.settings.uiKitId` в лаборатории
- **THEN** Workbench требует явного подтверждения project migration
- **AND** страница лаборатории не перезагружается
- **AND** Sandpack payload запрашивается повторно для текущего проекта

#### Scenario: Лаборатория показывает итог project migration
- **WHEN** migration project `UI kit` завершена
- **THEN** Workbench и preview показывают явный migration status
- **AND** пользователь видит, что текущий уровень нужно пройти заново, если migration сбросила его progress

#### Scenario: Пользователь включает режим html-tags
- **WHEN** пользователь выбирает режим без UI kit и `project.settings.uiMode` равен `html-tags`
- **THEN** лаборатория проверяет компонент на совместимость с HTML JSX-тегами
- **AND** пользователь остаётся в текущем рабочем контексте

#### Scenario: Лаборатория показывает диагностику несовместимости UI kit
- **WHEN** текущий проект и компонент несовместимы
- **THEN** лаборатория показывает диагностическое сообщение рядом с preview
- **AND** не роняет рабочую страницу целиком

#### Scenario: Лаборатория показывает runtime-диагностику Sandpack preview
- **WHEN** Sandpack preview сообщает runtime-ошибку после сборки payload
- **THEN** лаборатория показывает host-level диагностическое сообщение рядом с preview
- **AND** не отключает внутренний Sandpack error overlay

### Requirement: Sandpack preview публикует structured diagnostics payload build

Система SHALL возвращать из preview payload builder structured runtime diagnostics для build/cache/degradation paths, чтобы speed/load verdicts и локальная диагностика читали единый surface.

#### Scenario: Preview payload build возвращает cache и size diagnostics
- **WHEN** runtime собирает Sandpack preview payload
- **THEN** payload содержит structured diagnostics с `durationMs`, bounded cache hit/miss и size/load полями
- **AND** diagnostics позволяют оценить объём dependency/file build без внешней telemetry
- **AND** diagnostics различают reuse тяжёлых derived artifacts и CSS compile path

#### Scenario: Preview compatibility fallback помечается как degradation signal
- **WHEN** preview builder вынужден переключиться в compatibility fallback или safe component mode
- **THEN** structured diagnostics помечает этот путь как degraded
- **AND** downstream tests могут отличить обычную сборку от degraded branch

#### Scenario: Preview runtime guardrail помечает неподдерживаемый Server Action API
- **WHEN** preview builder находит в пользовательских preview-файлах Next.js Server Actions или форму с function-action
- **THEN** structured diagnostics помечает этот путь как degraded с причиной `unsupported_preview_api`
- **AND** пользователь получает безопасный fallback вместо каскада Next runtime-ошибок внутри iframe

#### Scenario: Preview budget exceed переводит runtime в safe degradation mode
- **WHEN** preview payload превышает budget по входному объёму или числу Tailwind candidates
- **THEN** runtime не запускает дорогую preview-сборку без ограничений
- **AND** пользователь получает безопасный fallback и structured diagnostics с причиной `preview_budget_exceeded`

### Requirement: Lab level может быть представлен как workflow step profile

Система SHALL мапить текущий lab level в workflow step profile без изменения пользовательского lab UX.

#### Scenario: Текущий lab level мапится в workflow step
- **WHEN** runtime строит projection для открытого уровня лаборатории
- **THEN** workflow step получает kind `level-lab`
- **AND** входы шага берутся из source image artifacts
- **AND** выходы шага берутся из текущих рабочих файлов, prompt history и check-result

### Requirement: Текущая лаборатория является Workbench profile

Система SHALL описывать текущую лабораторию компонента как первый profile общей Workbench platform без заметного изменения пользовательского UX.

#### Scenario: Lab workbench использует platform registry
- **WHEN** runtime описывает текущую лабораторию компонента
- **THEN** используется profile `level-lab` и definition `lab-component-workbench`
- **AND** редактор Monaco, Sandpack preview, prompt composer и lab controls подключены как tools registry
- **AND** текущие controls не требуют новой runtime dependency
